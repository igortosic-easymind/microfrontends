import('./src/bootstrap');
/**
This just gives Wabpack when it's running inside the browser a little bit of time to make sure that
we can load up all the appropriate dependencies to actually run our bootstrapped JS file.
 */